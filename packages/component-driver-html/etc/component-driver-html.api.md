## API Report File for "@atomic-testing/component-driver-html"

> Do not edit this file. It is a report generated by [API Extractor](https://api-extractor.com/).

```ts

import { ClickOption } from '@atomic-testing/core';
import { ComponentDriver } from '@atomic-testing/core';
import { HoverOption } from '@atomic-testing/core';
import { IClickableDriver } from '@atomic-testing/core';
import { IComponentDriverOption } from '@atomic-testing/core';
import { IDisableableDriver } from '@atomic-testing/core';
import { IFormFieldDriver } from '@atomic-testing/core';
import { IInputDriver } from '@atomic-testing/core';
import { IMouseInteractableDriver } from '@atomic-testing/core';
import { Interactor } from '@atomic-testing/core';
import { IReadonlyableDriver } from '@atomic-testing/core';
import { IRequirableDriver } from '@atomic-testing/core';
import { IToggleDriver } from '@atomic-testing/core';
import { IValidatableDriver } from '@atomic-testing/core';
import { Nullable } from '@atomic-testing/core';
import { Optional } from '@atomic-testing/core';
import { PartLocator } from '@atomic-testing/core';

// @public (undocumented)
export class HTMLAnchorDriver extends ComponentDriver<{}> implements IClickableDriver, IMouseInteractableDriver {
    click(option?: ClickOption): Promise<void>;
    get driverName(): string;
    getHref(): Promise<Optional<string>>;
    getTarget(): Promise<Optional<string>>;
    hover(option?: HoverOption): Promise<void>;
}

// @public (undocumented)
export class HTMLButtonDriver extends ComponentDriver<{}> implements IClickableDriver, IMouseInteractableDriver, IDisableableDriver {
    get driverName(): string;
    isDisabled(): Promise<boolean>;
}

// @public (undocumented)
export class HTMLCheckboxDriver extends ComponentDriver<{}> implements IFormFieldDriver<string | null>, IToggleDriver, IDisableableDriver, IReadonlyableDriver {
    get driverName(): string;
    getValue(): Promise<string | null>;
    isDisabled(): Promise<boolean>;
    isReadonly(): Promise<boolean>;
    isSelected(): Promise<boolean>;
    setSelected(selected: boolean): Promise<void>;
}

// @public (undocumented)
export class HTMLCheckboxGroupDriver extends ComponentDriver<{}> implements IInputDriver<readonly string[]> {
    get driverName(): string;
    getValue(): Promise<readonly string[]>;
    protected setSelectedByValue(value: string, selected: boolean): Promise<void>;
    setValue(values: readonly string[]): Promise<boolean>;
}

// @public
export class HTMLElementDriver extends ComponentDriver<{}> {
    get driverName(): string;
}

// @public (undocumented)
export class HTMLFileInputDriver extends ComponentDriver<{}> {
    constructor(locator: PartLocator, interactor: Interactor, option?: Partial<IComponentDriverOption>);
    get driverName(): string;
    uploadFiles(files: string | string[]): Promise<void>;
}

// @public (undocumented)
export class HTMLHiddenInputDriver extends ComponentDriver<{}> implements IInputDriver<string | null> {
    constructor(locator: PartLocator, interactor: Interactor, option?: Partial<IComponentDriverOption>);
    get driverName(): string;
    getValue(): Promise<string | null>;
    setValue(_value: string | null): Promise<boolean>;
}

// @public (undocumented)
export class HTMLOptionDriver extends ComponentDriver {
    get driverName(): string;
    label(): Promise<string | null>;
    value(): Promise<string | null>;
}

// @public (undocumented)
export class HTMLRadioButtonGroupDriver extends ComponentDriver<{}> implements IInputDriver<string | null> {
    get driverName(): string;
    getValue(): Promise<string | null>;
    setValue(value: string | null): Promise<boolean>;
}

// @public
export class HTMLRangeInputDriver extends ComponentDriver<{}> implements IInputDriver<number>, IDisableableDriver, IReadonlyableDriver {
    constructor(locator: PartLocator, interactor: Interactor, option?: Partial<IComponentDriverOption>);
    get driverName(): string;
    getValue(): Promise<number>;
    isDisabled(): Promise<boolean>;
    isReadonly(): Promise<boolean>;
    setValue(value: number): Promise<boolean>;
}

// Warning: (ae-forgotten-export) The symbol "ValueT" needs to be exported by the entry point index.d.mts
//
// @public (undocumented)
export class HTMLSelectDriver extends ComponentDriver<{}> implements IInputDriver<Nullable<ValueT>>, IDisableableDriver, IReadonlyableDriver {
    get driverName(): string;
    // (undocumented)
    getSelectedLabel(isMultiple: true): Promise<readonly string[] | null>;
    // (undocumented)
    getSelectedLabel(isMultiple: false): Promise<string | null>;
    // (undocumented)
    getSelectedLabel(): Promise<string | null>;
    getValue(): Promise<Nullable<ValueT>>;
    getValuesByLabels(labels: readonly string[]): Promise<readonly string[]>;
    isDisabled(): Promise<boolean>;
    isMultiple(): Promise<boolean>;
    isReadonly(): Promise<boolean>;
    selectByLabel(label: string | readonly string[]): Promise<void>;
    setValue(value: Nullable<ValueT>): Promise<boolean>;
}

// @public (undocumented)
export class HTMLTextAreaDriver extends HTMLTextInputDriver {
    get driverName(): string;
}

// @public (undocumented)
export class HTMLTextInputDriver extends ComponentDriver<{}> implements IInputDriver<string | null>, IDisableableDriver, IReadonlyableDriver, IRequirableDriver, IValidatableDriver {
    constructor(locator: PartLocator, interactor: Interactor, option?: Partial<IComponentDriverOption>);
    get driverName(): string;
    getValue(): Promise<string | null>;
    isDisabled(): Promise<boolean>;
    isError(): Promise<boolean>;
    isReadonly(): Promise<boolean>;
    isRequired(): Promise<boolean>;
    setValue(value: string | null): Promise<boolean>;
}

// (No @packageDocumentation comment for this package)

```
