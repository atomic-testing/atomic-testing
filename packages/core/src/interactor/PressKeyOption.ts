/**
 * Options for {@link Interactor.pressKey}.
 *
 * Intentionally empty for now. It exists so the `pressKey` signature can grow
 * modifier-key support (e.g. `ctrl`, `shift`, `alt`, `meta`) later without a
 * breaking change, mirroring the per-type option files such as {@link FocusOption}.
 */
export interface PressKeyOption {}
